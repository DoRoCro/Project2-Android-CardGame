<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codeclan.cardgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".G2P1CHWPlayActivity" />
        <activity android:name=".GameSelectActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".G2P1CHWResultsActivity" />
        <activity android:name=".G2P2CHWPlayActivity" />
        <activity android:name=".G2P2CHWResultsActivity" />
        <activity android:name=".G3CardBragActivity"></activity>
    </application>

</manifest>